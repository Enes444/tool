# SaaS-Ready scope in this package

## Included (technically in ZIP)
- One-click local run (build, migrate, bootstrap, checks)
- Alembic migration baseline
- Production compose with reverse proxy (Caddy), HTTPS, security headers
- Production env template and deployment script

## Still external (not zip-closable)
- Real DNS record pointing DOMAIN to server
- Legal docs finalization (AVV/DPA, privacy, imprint)
- Operational on-call ownership and alert channels
- Independent penetration/security audit
